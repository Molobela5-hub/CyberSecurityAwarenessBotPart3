<Window x:Class="CyberSecurityAwarenessBotPart3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CyberSecurityAwarenessBotPart3"
        mc:Ignorable="d"
        Title="Cybersecurity Awareness Bot 🔐" Height="550" Width="900"
        WindowStartupLocation="CenterScreen">

    <Grid Background="#1E1E1E">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock
        Text="Cybersecurity Awareness Bot"
        FontSize="28"
        FontWeight="Bold"
        Foreground="Cyan"
        Margin="20"/>

        <!-- Chat Area -->
        <ListBox
        x:Name="ChatListBox"
        Grid.Row="1"
        Margin="20,0,20,10"
        Background="#2D2D30"
        Foreground="White"
        FontSize="16"
        BorderBrush="Cyan"
        BorderThickness="2"
        ScrollViewer.VerticalScrollBarVisibility="Auto"
        ScrollViewer.HorizontalScrollBarVisibility="Disabled"/>

        <!-- Input Area -->
        <StackPanel
        Grid.Row="2"
        Orientation="Horizontal"
        Margin="20">

            <TextBox
            x:Name="UserInputTextBox"
            Width="600"
            Height="40"
            FontSize="16"
            Margin="0,0,10,0" 
            TextChanged="UserInputTextBox_TextChanged"
            KeyDown="UserInputTextBox_KeyDown"
            VerticalContentAlignment="Center"
            Padding="10,0,0,0"
            BorderBrush="Cyan"
            BorderThickness="2"/>

            <Button
            Content="Send"
            Width="120"
            Height="40"
            FontSize="16"
            Click="SendButton_Click"
            Background="Cyan"
            Foreground="Black"
            FontWeight="Bold"
            Cursor="Hand"/>

        </StackPanel>

    </Grid>
</Window>
